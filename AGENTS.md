# Working on Flick

Read INSTALL.md for installation and docs/reference.md for runtime contracts.

Use the TypeSafe skill for provider design and consult the live documentation at https://docs.typesafe.ai/llms.txt. Keep exact values, validation, and execution in code; use Jev for bounded semantic choices.

Build with npm run build. Run npm test for offline validation. Live tests need the user's TypeSafe key and use synthetic fixtures. Build the optional native helper with npm run build:native on macOS.

Keep MCP stdout clean. Preserve cancellation, observed target identity, and desktop ownership. Verify outcomes from the application. Count retries and extra model calls in measurements. Keep credentials, personal app data, and local browser profiles out of commits.
